# Hibernate-Mapping-Examples
Simple example code to demonstrate mapping of hibernate.

contains the DB schema in sql files in DB folder.
change your mysql credentials accordingly.

Some of the methods are commented out to simplify run process
